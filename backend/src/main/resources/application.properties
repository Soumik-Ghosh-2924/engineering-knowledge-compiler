# Server Configuration

server.port=8080

# Engineering Knowledge Compiler

compiler.workspace.root=${user.home}/.ekc/workspace
compiler.workspace.repositories=repositories
compiler.workspace.temporary=temporary

# Logging

logging.level.ekc=INFO
logging.level.org.eclipse.jgit=WARN

# Phase 2 remains disabled in the existing MVP unless explicitly enabled.
ekc.features.phase2.enabled=${EKC_PHASE2_ENABLED:false}
